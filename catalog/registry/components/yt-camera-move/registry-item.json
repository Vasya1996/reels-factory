{
  "$schema": "https://hyperframes.heygen.com/schema/registry-item.json",
  "name": "yt-camera-move",
  "type": "hyperframes:component",
  "title": "Camera Punch-In",
  "description": "Dynamic zoom, slide, and 3D tilt-pan helpers for any wrapper (video, include, card) with cubic-easing defaults and an edge-defocus pulse overlay",
  "tags": [
    "camera",
    "zoom",
    "effect",
    "creator"
  ],
  "files": [
    {
      "path": "yt-camera-move.html",
      "target": "compositions/components/yt-camera-move.html",
      "type": "hyperframes:snippet"
    }
  ],
  "reels": {
    "use_when": "Кадру нужен наезд или сдвиг: подойти ближе к тому, о чём речь.",
    "kind": "effect",
    "mount": "composition",
    "decor_texts": [],
    "text_slots": [],
    "skip": "тот же демо-хелпер-паттерн, что у hw-boil/mk-usage-arc/mk-emphasis-type: «self-preview stand-in \"footage\" (not part of the snippet)» (`yt-camera-move.html:46`) и «Self-preview timeline (not part of the snippet you copy)» (`yt-camera-move.html:163`), с той же жёсткой строкой `tl.set(\"#yt-cm-root\", { visibility: \"hidden\" }, 3.98)` (`yt-camera-move.html:170`) — код у всех четырёх позиций один и тот же по грепу `not part of the snippet you copy`. Честная оговорка: живой кадр (`hyperframes snapshot` на 1/3/5/7.5 с реальной сборкой) для ЭТОЙ позиции содержимое («FOOTAGE» плейсхолдер) не погасил ни на одном сэмпле — в отличие от остальных трёх, видимого исчезновения не воспроизвёл. Снято по совпадению кода (тот же автор, тот же паттерн, та же строка), а не по подтверждённому кадром симптому у этой конкретной позиции — открытый вопрос."
  }
}
