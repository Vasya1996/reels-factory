# Текст задачи для новой сессии

Скопировать в `/goal` целиком:

---

Доведи ускорение сборки рилса до приёмки. Задание — `docs/goal-hyperframes-speed.md`,
оно не менялось и действует целиком: следуй ему буквально, цель достигнута,
когда соблюдены все `<definition_of_done>`.

Состояние работы, замеры и найденные дефекты — `docs/handoff-speed.md`. Начни
с раздела «Дальше по порядку» и веди этот файл дальше по ходу работы: он
единственный след между сессиями.

Коротко, чтобы не перечитывать: конвейер переделан — агент отдаёт только план
(`cards` в `storyboard.json` плюс фрагменты карточек), композицию собирает код.
Одиннадцать гейтов проходят, `check` без ошибок, формат, длительность и
громкость в норме, стоимость сессии агента упала с 17,10 $ до 1,86 $. Не сошлись
два пункта приёмки: полная сборка идёт 37,5 минуты вместо 19, и в готовом ролике
8 смен картинки вместо 20 при двух положениях ведущей вместо трёх.

Правило прежнее: судить по кадрам готового mp4, а не по раскадровке. Побочные
находки писать в `docs/research/hyperframes/speed-side-findings.md` сразу, как
заметил.

---
