# Пилотный ролик HyperFrames — заметки

## Кириллица: доказательство

Задача 1 (`hf_fonts.py`) проверена тестами на уровне файлов шрифтов
(fontTools/getBestCmap). Здесь — проверка глазами того, что реально выходит
из их рендера, плюс контрольный негатив, доказывающий, что проверка вообще
способна поймать поломку.

### Проект

```
cd "C:\Users\123\Videos\Reels" && npx --yes hyperframes@0.7.70 init cyr-test --resolution portrait --example blank --non-interactive
```

Брифовая команда (`init cyr-test --resolution portrait --non-interactive`, без
`--example`) отбита самим CLI: «Non-interactive init requires --example,
--video, or --audio». Добавлен `--example blank` — ровно то, что предлагает
собственная подсказка CLI для пустого стартового проекта; версия и остальные
параметры не менялись.

### Композиция

Три строки текстом `hf_fonts.KAZAKH_PROBE` = `Привет, әлем: ғ қ ң ө ұ ү һ і`:

- **A** — `font-family:'Manrope'; font-weight:700` (наш `@font-face`, донор Noto Sans на 5 недостающих букв);
- **B** — `font-family:'Montserrat'; font-weight:900` (их канонический бандл, негативный контроль);
- **C** — `font-family:'Montserrat'; font-weight:800` (вес вне канонического 900-бандла).

CSS собран через `hf_fonts.fonts_css()`. Разметка приведена к контракту
HyperFrames (root `data-composition-id`/`data-start`/`data-width`/
`data-height`/`data-duration`, `class="clip"` на секции, регистрация
`window.__timelines`) — брифовый скелет использовал только `class="clip row a"`
и `data-duration` на `<body>`, что `lint` отклонил тремя ошибками
(`root_missing_composition_id`, `missing_timeline_registry`,
`timed_element_missing_clip_class`). Это ожидаемо по тексту брифа: «Ошибки про
`class="clip"` или `data-duration` — чинить разметку, а не игнорировать».
Сам текст трёх строк и `fonts_css()`/`KAZAKH_PROBE` не менялись.

`npx --yes hyperframes@0.7.70 lint .` → `0 errors, 0 warnings`.

`npx --yes hyperframes@0.7.70 check . --json` → `"ok": true` по всем секциям
(lint/runtime/layout/motion/contrast). В логе компилятора:

```
[INFO] [Compiler] Fetched 11 font face(s) for "Montserrat" from Google Fonts (cached to ...\fonts\montserrat)
[INFO] [Compiler] Injected deterministic @font-face rules for 1 requested font families
```

Это подтверждает механизм, который и должен сработать: `Montserrat` не имеет
собственного `@font-face` в `fonts_css()` (в `hf_fonts.DONOR_RANGES` есть
только `manrope` и `unbounded`), поэтому их инжектор шрифтов сам подставляет
канонический Google-бандл Montserrat — то самое каноническое поведение,
которое строка B должна разоблачить.

### Снимок

```
npx --yes hyperframes@0.7.70 snapshot . --at 1 --output snaps
```
→ `C:\Users\123\Videos\Reels\cyr-test\snaps\frame-00-at-1s.png`

### Вердикт по трём строкам (осмотрено глазами, Read на PNG)

- **Строка A** (Manrope 700, наш `@font-face`): весь текст — единый
  геометричный гротеск, кириллица и все девять казахских букв
  (ә ғ қ ң ө ұ ү һ і) на одном шрифте, без переключений начертания.
  Буквы читаются и не превращаются в пустые прямоугольники (tofu).
- **Строка B** (Montserrat 900, канонический бандл, негатив): базовое слово
  «Привет, әлем» отрисовано **другим, засечковым (serif) шрифтом** — заметно
  отличается от геометричного гротеска строки A и от собственного продолжения
  строки B. После двоеточия казахские буквы (ғ қ ң ө ұ ү һ) неожиданно снова
  сан-сериф — то есть даже внутри одной строки два разных шрифта. Это ровно
  та поломка, которую негатив должен ловить: на весе 900 у Montserrat
  Google-бандл, судя по всему, не несёт нормальной кириллицы вообще (только
  латиница), и браузер откатывается на системный засечковый шрифт для базовых
  русских букв, а для казахских добавочных берёт грани из другого
  зафетченного веса той же семьи, где кириллица есть.
- **Строка C** (Montserrat 800, вес вне 900-бандла): единый гротеск на всю
  строку, включая все казахские буквы — визуально ровно как у строки A по
  структуре (только другая гарнитура). Значит на этом весе у Google-Montserrat
  кириллица (в т.ч. cyrillic-ext) реально есть, и брифовое «C читается» не
  просто читается, а читается ОДНИМ шрифтом без разрывов.

**Негативный контроль сработал**: B заметно и однозначно хуже A —
переключение шрифта посреди строки, часть текста в чужом (засечковом,
непредназначенном) начертании. Проверка способна поймать поломку.

Снимок: `C:\Users\123\Videos\Reels\cyr-test\snaps\frame-00-at-1s.png`
(плюс покадровые вырезки строк для контроля:
`snaps\row_a_crop.png`, `snaps\row_b_crop.png`, `snaps\row_c_crop.png`).

Проект `cyr-test` — одноразовый, в `C:\Users\123\Videos\Reels\`, в git не
входит.

## Задача 3: доустановка жанровых скиллов

### `skills check` до установки

```
hyperframes skills
  Location  C:\Users\123\.claude\skills (claude-code)
  ✓ 9 current   ◦ 10 available on demand
  Available on demand (installed when their workflow first runs):
    ◦ embedded-captions
    ◦ faceless-explainer
    ◦ figma
    ◦ general-video
    ◦ motion-graphics
    ◦ music-to-video
    ◦ pr-to-video
    ◦ product-launch-video
    ◦ remotion-to-hyperframes
    ◦ slideshow
  ◇  Installed skills are up to date
```

**Расхождение с ожиданием брифа**: бриф предполагал на эту дату «4 актуальны,
5 устарели». По факту все 9 уже установленных скиллов на момент проверки были
актуальны (`Installed skills are up to date`), устаревших не было вовсе.
Возможные причины (не проверял) — более ранние задачи плана или отдельный
`skills update` уже подтянули их до latest. Записываю как факт, а не как то,
что бриф ожидал.

### Команда установки

```
npx --yes hyperframes@0.7.70 skills update product-launch-video faceless-explainer embedded-captions music-to-video
```

Вывод инсталлятора явно перечислил обновлёнными/поставленными только эти
четыре: `Installed/updated 4 skill(s): embedded-captions, faceless-explainer,
music-to-video, product-launch-video`. Про остальные пять (уже стоявшие)
инсталлятор ничего не сообщил отдельно — версии каждого скилла до команды я
не снимал (бриф просил сохранить именно вывод `skills check`, не версии
поштучно), поэтому не могу подтвердить и не могу опровергнуть, что они тоже
молча обновились до latest, как предполагало решение №2 брифа. Итоговый
`skills check` (ниже) показывает рост только на 4 новых, что согласуется и с
«пятёрка была и осталась актуальной», и не противоречит «пятёрка тоже
обновилась молча».

### `skills check` после установки

```
hyperframes skills
  Location  C:\Users\123\.claude\skills (claude-code)
  ✓ 13 current   ◦ 6 available on demand
  Available on demand (installed when their workflow first runs):
    ◦ figma
    ◦ general-video
    ◦ motion-graphics
    ◦ pr-to-video
    ◦ remotion-to-hyperframes
    ◦ slideshow
  ◇  Installed skills are up to date
```

Сравнение: 9→13 актуальных, 10→6 доступных по требованию. Разница — ровно
четыре запрошенных скилла (`embedded-captions`, `faceless-explainer`,
`music-to-video`, `product-launch-video`), подтверждено также листингом
`C:\Users\123\.claude\skills\` (15 директорий, включая инфраструктурные
`hyperframes*`, `media-use`, `skill-creator`, `talking-head-recut`,
`vael-reels`). Версия CLI не менялась — все команды выполнялись через
`npx --yes hyperframes@0.7.70`.

### Скрипты: путь из брифа/решения → фактический путь → существует

| Скрипт | Путь | Существует |
|---|---|---|
| `safe-zones.cjs` | `C:\Users\123\.claude\skills\embedded-captions\scripts\safe-zones.cjs` | да |
| `check-occlusion.cjs` | `C:\Users\123\.claude\skills\embedded-captions\scripts\check-occlusion.cjs` | да |
| `assemble-index.mjs` | `C:\Users\123\.claude\skills\product-launch-video\scripts\assemble-index.mjs` | да |
| `dimensions.mjs` | `C:\Users\123\.claude\skills\faceless-explainer\scripts\lib\dimensions.mjs` | да |
| `captions.mjs` | `C:\Users\123\.claude\skills\faceless-explainer\scripts\captions.mjs` **и** `C:\Users\123\.claude\skills\product-launch-video\scripts\captions.mjs` | да, в обоих скиллах (разные копии, не единый общий файл) |
| `transitions.mjs` | `C:\Users\123\.claude\skills\faceless-explainer\scripts\transitions.mjs` **и** `C:\Users\123\.claude\skills\product-launch-video\scripts\transitions.mjs` | да, в обоих скиллах |
| `preview-frames.cjs` | `C:\Users\123\.claude\skills\embedded-captions\scripts\preview-frames.cjs` | да |
| `transcribe.cjs` | `C:\Users\123\.claude\skills\embedded-captions\scripts\transcribe.cjs` | да |
| `resolve.mjs` (media-use) | `C:\Users\123\.claude\skills\media-use\scripts\resolve.mjs` | да |
| `transcript-cut.mjs` (media-use) | `C:\Users\123\.claude\skills\media-use\scripts\transcript-cut.mjs` | да |
| `analyze-beatgrid.py` (music-to-video) | `C:\Users\123\.claude\skills\music-to-video\scripts\analyze-beatgrid.py` | да |

Первые четыре пути совпали с путями из брифа буквально — подгонять
предположения не пришлось. `captions.mjs` и `transitions.mjs` оказались не
общими файлами, а отдельными копиями в двух скиллах каждый — задачи 4-6
должны явно указывать, из какого скилла берут файл.

### Точечные тесты движка (по решению, не полный набор)

```
cd plugins/reels-factory/engine && python -m pytest tests/test_hf_fonts.py tests/test_hyperframes_blocks.py -q
```
→ `27 passed`. Скиллы (тексты/скрипты на диске) движок не задели, как и
ожидалось.

### Сомнения

- Не подтверждено (и не опровергнуто) молчаливое обновление пяти ранее
  установленных скиллов версией `skills update` — инсталлятор отчитался
  только по четырём запрошенным именам, версии по одиночным скиллам до
  команды не снимались.
- `captions.mjs` и `transitions.mjs` существуют как минимум в двух местах
  каждый (`faceless-explainer` и `product-launch-video`) — не единый файл;
  дальнейшим задачам (4-6) нужно указывать конкретный скилл, а не имя файла.

## Задача 4: карта кадра их средствами против нашей

### Вырезка по альфе — буквальный бриф ловит фиктивную альфу

```
cd "C:\Users\123\Videos\Reels" && mkdir pilot-hf
npx --yes hyperframes@0.7.70 remove-background "work\bot-583558720-1784873847\avatar_0.mp4" -o "pilot-hf\alpha.webm"
```

Модель `u2net_human_seg.onnx` (168 МБ) скачалась однократно, команда шла в
фоне дольше пяти минут — дождался, не убивал (по решению из брифа).
`ffprobe` на выходе: `pix_fmt=yuv420p` (не `yuva420p` — буквы «a» нет), но в
`TAG:ALPHA_MODE=1` контейнер формально помечен как альфа-содержащий.
Проверил по пикселям, а не по тегу: `ffmpeg -vf alphaextract` на 9 кадрах,
покрывающих весь клип (0.5–11.5с). Уточнение после ревью: альфа-плоскости в
потоке **нет вообще** — `alphaextract` падает с `Requested planes not
available`. Значение 255 появляется только при принудительной конвертации в
`yuva420p`, где ffmpeg сам подставляет непрозрачность; это не измерение
канала, а следствие апконверта отсутствующего. Воспроизвёл отдельно на
изолированном 2-секундном тестовом клипе (`--device cpu --json`, провайдер
подтверждён через `--info`: на этой машине доступен только `cpu`) — тот же
результат. Тот же тестовый клип с `-o ...mov` (ProRes 4444) вместо `.webm`
дал реальную маску: `pix_fmt=yuva444p12le`, альфа 0–255 (57.6% нулей, 40.5%
255, 1.9% мягких краёв). **В версии `0.7.70` на CPU-провайдере этой Windows-
машины `remove-background` в `.webm`-выход альфу не пишет вообще, хотя
контейнер её заявляет; в `.mov`-выход — пишет настоящую.** Полный клип
(293 кадра, ~7 минут) пересчитан в `pilot-hf\alpha.mov` — на нём и построено
всё дальнейшее сравнение. `pilot-hf\alpha.webm` (буквальный бриф) оставлен
как есть — как доказательство дефекта, не как рабочий вход.

Это прямое противоречие интерфейсу задачи 4 («даёт `alpha.webm` — вход для
задач 5-6») — см. «Сомнения» ниже.

**Важная поправка после ревью — вес этой находки меньше, чем кажется.**
Эталонный скрипт скилла `matte.cjs` (строки 200–231) сам никогда не просит
`.webm`: он жёстко зовёт `remove-background ... -o *.mov` с комментарием
«ProRes 4444 keeps the alpha lossless». То есть штатный путь скилла этот
формат и так обходит стороной. Правильная формулировка находки — не «в их
CLI скрытый баг», а «шаг 1 нашего брифа вёл по формату, которым их
собственный конвейер не пользуется». Для задач 5-6 это означает простое
решение: работать с `.mov`, как делает их скрипт.

### Их расчёт (`safe-zones.cjs`) — воспроизведён контракт `matte.cjs`, не сам файл

`safe-zones.cjs` ожидает не видео, а `frames_fg/`+`frames_bg/`+`matte.fps`,
которые обычно готовит `matte.cjs`. `matte.cjs` ищет CLI по
`HYPERFRAMES_ROOT/packages/cli/dist/cli.js` (монорепо-чекаут) — такого на
машине нет (есть только опубликованный npx-пакет, layout `dist/cli.js` без
`packages/`). Запустить `matte.cjs` буквально не вышло; воспроизвёл его же
документированный контракт вручную тем же `ffmpeg`, что и в его коде:
`frames_bg` из `source.mp4`, `frames_fg` из рабочего `alpha.mov`
(`-pix_fmt rgba`), `matte.fps=25`. Паритет кадров сошёлся сразу (293/293).

```
HYPERFRAMES_ROOT="C:\Users\123\AppData\Local\npm-cache\_npx\63a659d4974b2d46" \
  node "C:\Users\123\.claude\skills\embedded-captions\scripts\safe-zones.cjs" "C:\Users\123\Videos\Reels\pilot-hf"
```

→ `coverage 46.5%`, `largest`/`top` — единственная зона на всю ширину, 0–20.8%
высоты; ниже — только боковые карманы (лев. 0-45.8%×0-25.9%, прав.
0-41.7%×70.4-100%), `recommendation: "embed"`, плюс палитра/резкость/свет.
`windows: []` — нет `transcript.json` (задача 5), сравнение глобальное.

### Наш расчёт (`face_detect.py`) — оказался захардкоженным дефолтом, не измерением

`face_box_for()` → `zoom.detect_face_anchor()` вернул `(0.5, 0.42)` — это
ровно дефолт кода на случай "лицо не найдено" (`zoom.py:73`), не измерение.
Проверил прямым вызовом: `_detect_faces_opencv` кидает
`ModuleNotFoundError: No module named 'cv2'` — `opencv` не объявлен
зависимостью `reels-factory-engine` ни в одном экстра (`pyproject.toml`
проверен целиком). Поставил `opencv-python-headless` (PyPI, `5.0.0.93`),
чтобы получить настоящее измерение: наткнулся на второй, независимый дефект
— `cv2.CascadeClassifier` не существует в этой сборке, `cv2.data.haarcascades`
указывает на пустую директорию (без единого `.xml`). Оба случая глотаются
одним `except Exception` в `detect_face_anchor()` → тот же дефолт. Удалил
пакет после проверки, в зависимости проекта не добавлял.

Оговорка после ревью: второй тезис — что именно `opencv-python-headless`
версии `5.0.0.93` не несёт каскадов — ревьюер независимо подтвердить не смог
(установка пакетов в ревью запрещена), и он расходится с обычной практикой
этого пакета. Опираться на него как на факт не следует. Главный вывод от
этого не меняется и подтверждён без него: в текущем окружении `cv2` не
установлен и зависимостью не объявлен, боевой вызов `hf_render.py:237` идёт
без подмены детектора, значит наш расчёт свободных зон **всегда** возвращает
дефолт `(0.5, 0.42)`, ни разу не измеряя кадр.

`free_bands()` на дефолте: верх 0–33.5% высоты + низ 50.4–100% высоты, обе на
всю ширину.

### Сравнение

Ключевое расхождение: наш расчёт объявляет нижнюю половину кадра (50.4–100%
высоты) полностью свободной; их `heroBands.profile` на этой же высоте
(topPct=50%) даёт `occPct=80.6%` (пик по профилю — 94.4% на topPct≈70.8%;
и учти разницу гранулярности: `occPct` меряется полосой ~12.5% высоты и ~92%
ширины кадра, а наши `free_bands` — полосами во всю ширину) — то есть по
факту это одна из самых занятых частей кадра за ролик (жесты/плечи). Оба
согласны только в одном — верх кадра безопасен (наш верх 0–33.5%, их
0–20.8%). Структурно наш модуль никогда не видит рук/плеч/жестов (только
квадрат вокруг лица) и никогда не считает по временным окнам, даже если
детектор лица работал бы. Их скрипт добавляет слои, которых у нас нет вовсе:
явный вердикт embed/fg, яркость зон (washout-риск), палитра фона, подсказка
блюра текста по глубине резкости, направление тени по свету.

**Вывод**: их путь по существу заменяет `face_detect.py`, а не дублирует
его формально — даже без учёта дефекта окружения с `cv2`, наш инструмент
структурно грубее (нет силуэта тела, нет разбивки по времени, нет
перцептивных сигналов).

### Сомнения

- `pilot-hf/alpha.webm` (буквальный вход задач 5-6 по интерфейсу задачи 4)
  на этой машине несёт фиктивную альфу (везде 255) — воспроизведено дважды.
  Рабочая маска существует только как `pilot-hf/alpha.mov`. Нужно решение:
  либо задачи 5/6 явно переходят на `.mov`, либо кто-то чинит webm-путь CLI.
- Причина поломки webm-альфы не локализована точнее, чем «`0.7.70`,
  `--device cpu`, эта Windows-машина» — GPU/CoreML провайдеры не проверял
  (недоступны здесь).
- Их заявленное преимущество (разбивка по временным окнам) в этом прогоне
  не проверено — нет `transcript.json` (задача 5); после его появления
  стоит пересчитать `safe-zones.cjs` и посмотреть, меняется ли вердикт
  по фразам.
- `matte.cjs` не запускался буквально (нет монорепо-чекаута для
  `HYPERFRAMES_ROOT`) — воспроизведён его контракт вручную тем же
  `remove-background`/`ffmpeg`, не «прогнал их пайплайн одной кнопкой».

## Задача 5: пословный транскрипт — их два пути против нашего

### Звук

```
ffmpeg -y -i "...\work\bot-583558720-1784873847\avatar_0.mp4" -vn -ac 1 -ar 16000 "pilot-hf\voice.wav"
```
→ `pilot-hf\voice.wav`, моно 16 кГц, 11.71 с.

### Путь A — `hyperframes transcribe` (CLI, движок `whisper`): не работает на этой машине

```
cd pilot-hf && npx --yes hyperframes@0.7.70 transcribe voice.wav --engine whisper --model large-v3 --language ru
```
→ `Captions skipped — whisper-cpp not found. Install: Build with cmake: https://github.com/ggml-org/whisper.cpp#building`
(exit 1; с `--json` — `{"ok":false,"skipped":true,"reason":"whisper_unavailable"}`).

`npx --yes hyperframes@0.7.70 doctor` подтверждает: `✗ whisper-cpp Not found (optional — needed for transcription) — Build with cmake`.
На машине нет ни `cmake`, ни `HYPERFRAMES_ROOT` (монорепо-чекаута с `packages/cli/dist/cli.js`, см. Сомнения задачи 4) — то есть у их собственного whisper.cpp-фолбэка в `transcribe.cjs` (см. путь B) здесь тоже не было бы шанса сработать. **Путь A на этой машине непроходим без отдельной установки компилятора C/C++ — это не «модель докачать», а «собери бинарник»; за рамки того, что бриф считал нормальным ожиданием, я не выходил.**

### Путь B — `transcribe.cjs` (скилл, WhisperX): сработал после установки `uv`

`transcribe.cjs` по умолчанию идёт через WhisperX: `uvx --python 3.12 --from whisperx==3.8.6 whisperx ...`. `uvx` на машине не был на `PATH` (сам пакет `uv` уже стоял через pip — `python -m pip install uv` вывел `Requirement already satisfied: uv`; добавил `...\Python311\Scripts` в `PATH` только на сессию, системный `PATH` не менял).

Подготовка проекта под контракт скрипта (`<project>/source.mp4`):
```
mkdir pilot-hf\cjs-run && copy work\...\avatar_0.mp4 pilot-hf\cjs-run\source.mp4
node "...\skills\embedded-captions\scripts\transcribe.cjs" "pilot-hf\cjs-run" large-v3 ru
```

Первая попытка (холодный `uv`-кэш: докачка Python 3.12, `whisperx`, `torch`, модели выравнивания `jonatasgrosman/wav2vec2-large-xlsr-53-russian`, ~2 ГБ) не досчиталась до конца — через несколько минут все процессы (`python`/`uvx`/`uv`) на машине пропали без строки в лог и без кода ошибки; причина не локализована (не хватило терпения ждать через уведомление монитора, а не опрашивать файл напрямую — сам себе создал видимость зависания). Вторая попытка с тёплым кэшем (Python/torch/модели уже на диске) дошла до конца за ~2.5 минуты возобновляемого времени (по логам роста памяти процесса `python.exe`: загрузка `large-v3` → ASR → выгрузка → загрузка `wav2vec2-ru` → выравнивание → запись):

```
[transcribe] whisperx(large-v3+wav2vec2) 26 words → pilot-hf\cjs-run\transcript.json
[transcribe] text: Все продажи на свете сводятся к трем вопросам, и порядок этих
вопросов решает все. Мы обожаем усложнять продажи, учим хитрые приемы, зубрим
скрипты, ищем волшебные фразы.
```

Результат скопирован как `pilot-hf\transcript.json` (интерфейс задачи 5 — вход для задачи 6). `language_code: "ru"`, 26 слов.

### Наш путь — `transcribe.py` (faster-whisper)

Вызвана напрямую функция `transcribe(wav, "large-v3", "ru", "auto")` из
`reels_factory.transcribe` (не `transcribe_file` — та пишет фиксированное имя
`words.json` в тот же каталог, где уже лежал бы `transcript.json`; чтобы не
перезатирать чужой вывод, дёрнул внутреннюю функцию и сам сохранил в
`ours-words.json`):

```
python.exe -m python: from reels_factory.transcribe import transcribe
words, dev, lang = transcribe(r"pilot-hf\voice.wav", "large-v3", "ru", "auto")
```
→ `[transcribe] FAIL cuda: Library cublas64_12.dll is not found or cannot be loaded` (известная особенность машины, см. CLAUDE.md), откат на CPU:
`[transcribe] OK device=cpu lang=ru words=26` за 208.0 с (загрузка модели +
инференс на CPU, `large-v3` был закэширован заранее — докачки не было).

### Проверка: расшифровка, а не перевод

Оба пути вернули `language: "ru"` и **буквально совпадающий список из 26 слов
в одном порядке** — реальные русские слова связного текста о продажах
(«Все продажи на свете сводятся к трем вопросам…»), без единого латинского
symbol и без транслитерации. Расшифровка, не перевод — подтверждено текстом,
не только полем `language`.

### Сравнение границ (по существу, не только счёт слов)

Число слов совпало точно: **26 = 26**, тексты совпали пословно (сопоставление
по нормализованному тексту, `difflib` в порядке следования — все 26 слов
совпали как пары).

| Метрика (по 26 совпавшим словам) | Значение |
|---|---|
| Расхождение начала слова, среднее | 0.193 с |
| Расхождение начала слова, худшее | 0.455 с (слово «ищем») |
| Расхождение конца слова, среднее | 0.170 с |
| Расхождение конца слова, худшее | 0.554 с (слово «все.») |

Худшие 5 по началу: `ищем` (0.455), `Мы` (0.454), `зубрим` (0.450), `учим`
(0.443), `и` (0.364) — все пять стоят сразу **после** внутренней паузы/знака
(конец фразы или запятая перед ней).

Структурная причина вскрылась при разборе зазоров между соседними словами
(`start[i+1] - end[i]`) — не только абсолютных таймкодов:

| | наш (faster-whisper) | их (WhisperX) |
|---|---|---|
| Зазоров ровно `0.0` с | 21 из 25 | 0 из 25 |
| Диапазон ненулевых зазоров | 0.02–0.12 с (4 случая) | 0.02–0.061 с (все 25) |

Наш пословный таймкод у faster-whisper почти всегда **склеен впритык**:
конец слова буквально равен началу следующего (`0.0` с зазора), даже на
границе предложения («вопросам,»→«и», «все.»→«Мы» — 0.12 с, редкое
исключение). WhisperX (wav2vec2 forced alignment) везде оставляет небольшой,
но ненулевой зазор — то есть само выравнивание видит микропаузы в звуке,
а не только делит длительность сегмента поровну между словами.

### Вывод: какой путь точнее и стоит ли менять наш

- **Качество распознавания слов — на этом клипе одинаковое**: тот же язык, тот
  же порядок, те же 26 слов буква в букву, без транслитерации. На этом
  единственном материале нет оснований менять движок ради точности слов.
- **Граничная точность — разного типа, не «лучше/хуже» без оговорки.** Измерил
  разницу МЕЖДУ системами (таблица выше) — не сверял её с истинным звуком на
  слух (не делал независимой сверки по волне), это честная оговорка, а не
  измерение точности против истины. Но структурный факт (21/25 нулевых
  зазоров у нас против 0/25 у них) измерен напрямую и говорит о разной природе
  таймкодов: наш способ (внутренняя атеншен-эвристика faster-whisper) не
  моделирует межсловные паузы вообще, WhisperX (forced alignment) — моделирует.
  Комментарий в самом `transcribe.cjs` называет причину так же: «wav2vec2
  forced alignment gives word timings far tighter than whisper.cpp's
  segment-interpolated ones» — это их формулировка/дизайн-решение, привожу как
  цитату их обоснования, не как собственное измерение.
- **Путь A (`hyperframes transcribe`, whisper.cpp) на этой машине неприменим
  вообще** — нужен компилятор C/C++ (`cmake`), которого нет; сравнивать его
  точность здесь невозможно физически.
- **Итог**: на одном 12-секундном клипе менять `faster-whisper` на WhisperX
  ради точности слов — не обосновано (слова совпали идеально). Если когда-то
  понадобится точное определение пауз/зазоров именно по ASR-таймкодам (не по
  `silencedetect`, как сейчас делает `transcribe.cjs` для хвостовых
  галлюцинаций) — наш путь для этого структурно не годится (зазоры почти
  всегда 0), а добавлять forced-alignment слоем НАД faster-whisper дешевле,
  чем переезжать на WhisperX целиком (плюс инфраструктура: `uv`, ~2 ГБ
  докачки на первый прогон, ~2.5 мин CPU против наших 208 с — сопоставимо,
  выигрыша по скорости у них на этой машине не увидел).

### Сомнения

- Путь A не проверен по факту вообще (не собирал `whisper-cpp` — нужен
  `cmake`/тулчейн, за рамки задачи) — сравнение точности границ = только
  faster-whisper против WhisperX, третьей точки нет.
- Первый прогон `transcribe.cjs` не досчитал и не оставил ни строки лога, ни
  кода ошибки — процессы просто исчезли через несколько минут. Не
  локализовано, баг это скрипта/`uv` на этой машине или следствие того, как я
  сам ждал (создавал фоновые мониторы вместо прямого опроса файла). Второй
  прогон с тёплым кэшем прошёл штатно — воспроизвести падение не пытался
  (не хотел жечь время на повтор докачки).
- Расхождение границ измерено МЕЖДУ двумя ASR-системами, не против истинных
  границ звука (не сверял на слух/по спектрограмме) — вывод о «структурно не
  моделирует паузы» обоснован зазором `0.0`, а не тем, что я проверил, где
  пауза была на самом деле.
- Казахский путь выравнивания в `transcribe.cjs` не проверялся вообще (по
  решению брифа — нет материала на kk). Сам файл `transcribe.cjs` не хранит
  таблицу «язык → модель выравнивания» — она живёт внутри пакета `whisperx`
  (`whisperx/alignment.py`, не открывал), поэтому подтвердить или опровергнуть
  наличие/отсутствие kk-модели выравнивания из уже виденного кода не могу —
  это не измерение, а честный пробел.
- Для запуска пути B установил `uv` через `python -m pip install uv` (пакет
  уже был на диске, не был только на `PATH` в этой сессии) — не системная
  правка, но новая точка опоры окружения, которой не было в самом брифе;
  выполнил её как продолжение решения №1 брифа («если требует скачивания
  модели — это нормально, дождись»), а не спрашивал отдельно.

## Задача 6: пилотный ролик их процессом

Собран вертикальный рилс целиком конвейером HyperFrames на архивном клипе.
Итог: `C:\Users\123\Videos\Reels\pilot-hf-2026-07-31.mp4` — 1080×1920, h264,
30 fps, aac 48 kHz stereo, 11.733 s (задумано 11.72, расхождение +13 мс),
8.2 МБ. Проект — `C:\Users\123\Videos\Reels\pilot-hf`, в гит не идёт.
HeyGen не вызывался, CLI везде `npx --yes hyperframes@0.7.70`.

### Числа из их справочников, которым следовала сборка

| Величина | Их значение | Источник | Что в сборке |
|---|---|---|---|
| доля главного элемента | ≥ 40 % холста | `visual-design.md` → Density | ведущая 46.5 % (`safe-zones.coverage`) |
| слои глубины | ≥ 3 | там же | ровно 3: видео / скрим / типографика |
| иерархия | ≥ 2 признака из size 3:1, weight 800 vs 400, contrast, position, motion | там же | 3 признака: 190 px vs 42 px, Unbounded 800 vs Manrope 600, canvas vs accent |
| приёмы глубины | 2–3 из size / blur / opacity / overlap / shadow | там же | 3: градиент, перекрытие, тень |
| длительность входа `POP_DUR` | 0.4–0.7 s, герой виден к t ≤ 0.5 s | `rules/spring-pop-entrance.md` | 0.42 / 0.46 / 0.50 / 0.55 s |
| стаггер | 0.04–0.08 s, `min(0.06, 0.5/N)`, `N × STAGGER ≤ 0.5 s` | там же | 0.05 s × 7 = 0.35 s |
| подъём `Y_RISE` | 0–32 px | там же | 20–26 px |
| пружинные кривые | `back.out` / `bounce.out` / `elastic.out` запрещены как дефолт, дефолт `power3` | `motion-language.md` ч. 2 п. 1 | ноль пружин; только power2 / power3 / expo / none |
| слов на кадр | 1–2 предложения, обычно 6–20 слов | `story-design.md` → VO_MODE | 8 / 6 / 12 |
| длина хука | 3–5 s | там же | 3.0 s |
| полоса субтитров | `CAPTION_BAND_FRACTION = 0.1667` → 320 px, `bandTopY = 1600` | `lib/dimensions.mjs` | **перенесена** на y 1140–1440 |
| контент кадра | верхние ~83 % | `visual-design.md` | верхние 21 % (y ≤ 400) — строже, по карте зон |
| вертикальный якорь героя | y ≈ 0.42 × H = 806 | `faceless-explainer/visual-design.md` → Portrait | **не применимо**: 806 = центр лица (`face.json cy 806`) |
| разрыв группы субтитров | `SILENCE_GAP = 0.18 s`, `TAIL_PAD = 0.12 s` | `captions.mjs` | 12 групп из 26 слов |
| потолок слов в группе | `>3.5 → 2`, `>2.5 → 3`, иначе 4 (окно 1.0 s) | там же | группы по 2–3 слова |
| пик блюра на склейке | 10 px для текста, 18–20 px для полного кадра | `cut-catalog.md` | 10 px |
| zoom-through | выход 0.2 s `power3.in` scale 1→1.2; вход 0.5 s `expo.out` scale 0.75→1 | `cut-catalog.md` §1 | воспроизведено дословно, кроме opacity на склейке |
| тайминги субтитров | группа 0.12 s / 0.10 s, плашка 0.15 s, все `power2` | `caption-highlight` | взято как есть |

### Композиция

Три кадра-подкомпозиции над сквозной подложкой: `01-tri-voprosa` (0–2.994,
«3 ВОПРОСА»), `02-poryadok` (2.994–5.364, «ПОРЯДОК» → zoom-through →
«РЕШАЕТ ВСЕ»), `03-uslozhnyaem` (5.364–11.72, кикер и растущий список из трёх
рядов). Границы кадров посажены в межсловные паузы 20 / 10 мс. Три разных
фрейминга. Вся графика — в `zones.largest` (1080×400 сверху), лицо
(cx 540, cy 806, h 269) не перекрыто, плашка субтитров ниже подбородка и выше
рук. Шрифты только из `hf_fonts.fonts_css()` (24 `@font-face`, data-URI);
стиль субтитров — `caption-highlight` из `ALLOWED_CAPTION_STYLES`; ни одной
темы из `BLOCKED_CAPTION_THEMES`. Палитра — из `safe-zones.palette`.

### Гейты

`lint` → 0 ошибок (по пути починены `multiple_root_compositions` и
`caption_exit_missing_hard_kill`). `check` в форме строже брифа —
`--frame-check "severity=error;seek=.25,.5,.75;tol=2"`,
`--caption-zone "x0=0;y0=.594;x1=1;y1=.75;severity=error;seek=.5,1"`,
`--at-transitions` — даёт Runtime 0/0, Layout 0/0, Motion 0/0,
Contrast 19/19 WCAG AA, **Check passed**. `transitions verify` → 1 переход
подтверждён. Ни одно нарушение не подавлено флагом: 7 ошибок
`content_overlap` на шве кроссфейда починены сменой перехода на жёсткую
склейку, warning контраста 2.11:1 — тем, что последняя группа субтитров
больше не гаснет на финальном кадре.

Осмотрено глазами: 13 снимков композиции, контакт-листы, шов кроссфейда и
5 кадров, извлечённых ffmpeg из готового MP4. Кириллица целиком на месте
(Ж, Щ, Ш, Ы, Я, запятые, точка); казахских букв в этом материале нет. Лицо и
жестикуляция не перекрыты. Замер яркости верхней полосы: 181.5 в исходнике →
66–77 с графикой (скрим работает и он именно градиент).

### Срослись ли жанры

**Частично.** `product-launch-video` вынес весь конвейер: `captions.mjs`,
`transitions.mjs`, `assemble-index.mjs` выдержали русский текст, живое видео и
вертикаль. От `faceless-explainer` пригодился **только** `lib/dimensions.mjs`
(пресет `portrait` и формула полосы) — его вертикальная доктрина не
перенеслась вообще, а шли мы к нему именно за ней.

Шов ровно один, и он в порядке слоёв: `assemble-index.mjs` выводит поднятое
видео **после** кадров-клипов и **без `z-index`**, поэтому ведущая закрашивает
всю графику (первый снапшот показал только субтитры). Правило
`.scene { z-index: 5 }` обязано лежать в сгенерированном `index.html` — из
`<style>` подкомпозиции оно не достаёт до хостового стека (CSS подкомпозиции
скоупится, проверено снапшотом). Хука между `assemble-index` и рендером их
конвейер не даёт, отсюда отдельный `postassemble.mjs`.

Остальные швы (полный разбор — в отчёте задачи): `assemble-index` переписывает
файл кадра на диске, вырезая поднятое видео, — второй прогон соберёт ролик без
ведущей и промолчит; непрерывной подложки в модели кадров нет (сквозное
11.72-секундное видео объявлено внутри 2.994-секундного кадра);
`captionBand(1920)` кладёт субтитры ровно в зону рук (74–94 % занятости);
вертикальные правила `faceless-explainer` написаны для пустого холста и на
говорящей голове ломаются (их якорь y ≈ 806 — это центр лица, «≥ 3 фрейминга»
непостроимо при единственной чистой полосе); каталожные блоки монтируются, но
4 из 6 свёрстаны под 1920×1080, а `lt-clean-bar` в вертикали садится карточкой
ровно в зону рук (проверено пробным монтированием со снапшотом и откатом);
`cut-catalog.md` рассчитан на проигрываемую ленту, а не на seek (его
`opacity: 0.15` на склейке даёт призрак входящего элемента, потому что
`fromTo` применяет from-состояние при сборке); громкость никто не нормализует —
готовый файл имеет ровно уровень исходника (−33.9 dB против −33.8 dB).

Отдельно: у них **есть** скилл ровно под наш жанр — `talking-head-recut`
(9:16, «the clip plays untouched underneath», карточки-оверлеи по транскрипту).
Его канонический пример — одна плоская композиция с видео первым в DOM, без
ассемблера и без кадров-подкомпозиций, то есть главный шов отсутствует по
конструкции. Цена: у него нет ни `captions.mjs`, ни реестра переходов, ни
`STORYBOARD.md`, а 35 карточных лейаутов свёрстаны под латиницу.

### Мины в самом инструменте (0.7.70)

- голый `--frame-check` валит `check` сообщением про **другой** флаг
  (`Invalid --caption-zone`) — рабочая форма только со значением;
- `add a b c` принимает только первое имя и молча игнорирует остальные;
- `add` даёт `EBUSY` **после** успешной записи файла — блок на месте, а вывод
  выглядит как провал;
- `snapshot --at 1 --at 5` молча оставляет последний; нужен список через запятую;
- `assemble-index` печатает `repaired ... - null`, когда единственной правкой
  был hoist видео;
- **комментарий в CSS с литералом тега v-i-d-e-o беззвучно отключает hoist**:
  их `scan` затирает тела `<style>`, но regexp идёт по сырому HTML, матч
  начинается внутри комментария и проглатывает настоящий элемент;
- `tl.to`, чей стартовый параметр пишется при первом рендере, **не переживает
  изолированный seek** (плашка субтитров осталась гореть на уже произнесённом
  слове при `--at 1.0`). Правило, выведенное и заложенное в скин: на элемент —
  ровно один `fromTo`, всё остальное — смена класса через `tl.set`.

### Сомнения

- Сквозное видео объявлено внутри короткого кадра: контракт формально не
  нарушен, но если hoist начнёт клампить по окну кадра, сборка развалится молча.
- `postassemble.mjs` привязан к точному тексту CSS-правила их ассемблера.
- Три голосовых среза вместо одной дорожки — следствие того, что escape-люк
  `data-frame-video="approved"` существует только для видео; на другом
  материале граница кадра может прийтись на слово.
- Кроссфейд безопасен только потому, что кадр 3 пуст на склейке — это свойство
  раскадровки, не гарантия конвейера.
- «≥ 40 % холста» я трактовал как ведущую; буквально их правило — про графику.
- Акцент `#C8722F` — производный от `palette.accentSuggestion #8c6146`
  (поднята светлота ради контраста на тёмном пиджаке), то есть дизайнерское
  решение, а не факт из материала.
- Ролик тихий (−33.9 dB): звук сознательно не трогал, но в продукт такой
  уровень отдавать нельзя.
- Рендер 30 fps при исходнике 25 fps (команда из брифа) — ресемплинг движения
  покадрово не мерил.
- Шесть блоков шага 2 установлены и проверены на монтируемость, но в ролик не
  вошёл ни один: содержательно это англоязычные лоуэр-трети, TikTok-подписка и
  скрапбук-дрессинг. Подставлять «Dr. Maya Chen» в русский рилс про продажи я
  не стал — если требовалось именно использовать их в кадре, эта часть не
  выполнена сознательно.

## Задача 7: приёмка и отчёт по пилоту

### Шаг 1 — сравнение глазами

Кадры вытащены `ffmpeg` на семи сопоставимых секундах (0.5, 2, 4, 6, 8, 10,
11.3 с) из обоих роликов и сведены в парные контакт-листы. Сопоставимый
диапазон — **только первые 11.72 с старого ролика**: `scenario.timed.json`
старой сборки показывает, что это ровно блок `hook` («Все продажи на свете
сводятся к трём вопросам… ищем волшебные фразы»), тот же текст, что и весь
пилотный HyperFrames-ролик (11.733 с). Дальше в старом ролике идут ещё три
блока (development/payoff/cta, итого 41.508 с) — их пилот не покрывает, не
сравниваю.

Осмотрено (не измерено программой, глазами по PNG/JPG):

- **Живость и лицо.** В обоих роликах лицо и жестикуляция ни разу не
  перекрыты — один и тот же кадр без монтажных склеек внутри 11.7 с, разница
  только в графическом слое поверх. Мёртвых участков без движения на
  проверенных секундах не нашёл ни в одном из двух — на всех кадрах руки в
  движении (жесты «загибает пальцы», ладони раскрыты).
- **Субтитры.** Старый ролик кладёт целую фразу одной строкой без разбивки и
  без выделения слов («ЗУБРИМ СКРИПТЫ, ИЩЕМ ВОЛШЕБНЫЕ ФРАЗЫ.» — 5 слов в
  одной плашке на t=11.3с). Пилот режет ту же фразу на группы по 2-4 слова
  («ИЩЕМ ВОЛШЕБНЫЕ» / «ФРАЗЫ.») с оранжевой плашкой-хайлайтом на опорном
  слове — короче строка, легче читать на бегу.
- **Графика.** Старый ролик — только субтитры, никакой другой графики за все
  11.7 с. Пилот добавляет три слоя поверх того же видео: заголовок-кикер
  вверху («3 ВОПРОСА» с цифрой на t=2s, «ПОРЯДОК» на t=4s, «УСЛОЖНЯЕМ
  ПРОДАЖИ» на t=6-11с), растущий чек-лист из трёх пунктов (t=8с — один пункт,
  t=11.3с — три), сдвинутая плашка субтитров. Вся графика лежит в верхних
  ~20% кадра и в кадр лица/рук ни разу не заходит — визуально подтверждает
  цифры из задачи 6 (`zones.largest` 1080×400 сверху, лицо cy=806 не
  перекрыто).
- **Громкость.** `ffmpeg -af volumedetect` на сопоставимом отрезке:
  пилот −33.9 dB (mean) / −16.8 dB (max), старый −33.9 dB / −17.1 dB —
  практически идентично. Ни одна из сборок звук не нормализует; это
  унаследованная особенность источника, не регресс ни в одну сторону.
- **Техника.** Оба 1080×1920, 30 fps, h264/aac. Пилот 8.2 МБ/11.73 с, старый
  13.0 МБ/41.57 с (на сопоставимом 11.7-секундном отрезке старый тяжелее —
  видимо, другой битрейт кодирования, не влияет на картинку на глаз).

**Вывод глазами: стало лучше.** На идентичном материале и идентичном тексте
пилот добавляет типографическую иерархию (кикер/заголовок/растущий список),
короче и читаемее субтитры, не теряя ни жеста, ни секунды присутствия
ведущей. Старый ролик не хуже технически, но визуально беднее — плоские
субтитры на статичном кадре. Само по себе устранение «голых субтитров на
говорящей голове» — это именно то усиление, ради которого была наша
кириллица-миграция на HyperFrames.

Файлы сравнения: `C:\Users\123\AppData\Local\Temp\claude\C--Users-123-projects-reels-factory\f0e412b4-004a-484a-b896-1d02d39aaae4\scratchpad\frames\pair_*.jpg`
(не в git, одноразовые).

### Шаг 2 — стоимость ролика в модели «агент рисует каждый кадр»

Данные (из брифа задачи, не из логов сессии напрямую — сырых JSON с разбивкой
input/output/cache в артефактах задачи 6 нет, только agregated-числа):
сборка (Задача 6, Opus 5) — 397 162 токена, 149 вызовов инструментов,
57 минут; проверка результата отдельной сессией (Sonnet 5) — 245 726
токенов.

Цены — из справочника навыка `claude-api` (кэш 2026-06-24, сверено с
таблицей текущих моделей): **Opus 5** $5.00 / $25.00 за 1М токенов
(вход/выход); **Sonnet 5** $3.00 / $15.00 за 1М, но по вводной цене
**$2.00 / $10.00** до 2026-08-31 включительно — сегодня 2026-07-31, вводная
цена ещё действует.

**Честная оговорка.** Данные дают только суммарный счётчик токенов на
сессию, без разбивки на input/output/кэш-чтение/кэш-запись, а тарифы для
этих категорий отличаются в 5-12.5 раз. Точную цену без этой разбивки
посчитать нельзя — дальше вилка, а не измерение:

| Сессия | Токены | Модель | Мин. (весь объём по цене входа) | Макс. (весь объём по цене выхода) |
|---|---|---|---|---|
| Сборка (задача 6) | 397 162 | Opus 5 | $1.99 | $9.93 |
| Проверка | 245 726 | Sonnet 5 (вводная цена) | $0.49 | $2.46 |
| **Итого** | 642 888 | — | **$2.48** | **$12.39** |

Агентные сессии с 149 вызовами инструментов обычно смещены к нижней границе:
основной объём токенов — это повторно отправляемый контекст, который при
включённом кэшировании читается по ~0.1× базовой цены входа, а не по полной
цене выхода. Это довод, а не измерение — точную цифру внутри вилки может
дать только `/cost`-подобный отчёт самой сессии (или `usage.iterations` API),
которого в переданных данных нет.

**Практический вывод для решения**: даже по верхней границе вилки одна
сборка ($1.99–$9.93 без проверки, $2.48–$12.39 с проверкой) сопоставима по
порядку величины со старым подходом ($9.79 за сборку демо-ролика 29 июля), а
по вероятной нижней границе — дешевле в разы. Резкого удорожания при переходе
на модель «агент собирает через HyperFrames-скиллы» не видно; для точной
цифры нужен реальный `/cost`-снимок следующей сборки.

### Шаг 3 — три вопроса спеки

**1. Срослись ли два жанровых процесса?**
**Частично** — дословно вывод задачи 6, подтверждаю по фактам пилота и по
собственному просмотру кадров. `product-launch-video` вынес весь конвейер
(`captions.mjs`, `transitions.mjs`, `assemble-index.mjs`) и выдержал русский
текст, живое видео, вертикаль — картинка на выходе объективно лучше старой
(шаг 1). Но сращивание потребовало ручного вмешательства не по мелочи: один
структурный шов (видео, поднятое `assemble-index.mjs` без `z-index`,
закрашивало графику — потребовался отдельный `postassemble.mjs`), плюс от
`faceless-explainer` пригодилась только формула полосы субтитров
(`lib/dimensions.mjs`), вся его вертикальная доктрина («≥3 фрейминга»,
центр-лица-якорь) для говорящей головы не подошла и не использовалась.
Итог не «сшили и работает само», а «сшили руками, зная где будет рваться».

**2. Сколько стоит ролик?**
$2.48–$12.39 за сборку+проверку (см. шаг 2), с уходом веса к нижней границе
на агентных сессиях с кэшированием. Точную цифру без разбивки
input/output/cache получить нельзя — это не уклонение от ответа, а
измеримый предел того, что дано в данных задачи.

**3. Какие их правила конфликтуют с нашими решениями?**
Оба заранее известных конфликта подтверждены по коду, не только со слов
брифа:

- **Пружинные входы.** Наш собственный каталог блоков
  `plugins/reels-factory/engine/src/reels_factory/hyperframes_blocks.py`
  использует `ease: "back.out(...)"` как явный дефолт минимум в 11 местах
  (строки 86, 177, 180, 259, 394, 427, 431, 467, 508, 511, 551) — бейджи,
  крупные числа, стрелки, свап-анимации, стаггер-списки. Их `motion-language.md`
  ч.2 п.1 (см. задачу 6) запрещает `back.out`/`bounce.out`/`elastic.out` как
  дефолт и требует `power3`. Пилот это подтвердил на практике: собирая
  ролик их процессом, агент **не использовал `hyperframes_blocks.py`
  вообще** — все анимации кадров 6 написаны заново на
  `power2`/`power3`/`expo`/`none` в обход собственного каталога. То есть
  конфликт не гипотетический: наш самый используемый каталог анимаций
  систематически ломает их правило, и обойти это можно только не пользуясь
  каталогом.
- **Потолок слов/секунд на кадр.** Разница глубже, чем несовпадение чисел.
  Их модель контента дискретна: `story-design.md` → VO_MODE режет сценарий
  на «кадры» с бюджетом ~6-20 слов и 3-9 с каждый (в задаче 6 — 8/6/12 слов
  на кадр, хук 3.0 с). Наша модель сценария (`scenario.py`) считает бюджет
  словами **на весь ролик** (`WORDS_PER_SECOND_TARGET = 2.5`,
  `words_soft`/`words_hard` — строки 32-119, суммарный, а не покадровый лимит)
  и не знает понятия «кадр» с собственным бюджетом вообще — оно появляется
  только на стороне HyperFrames-брифа (`hf_brief.py`), где границы кадров и
  раскладку по кадрам агент придумывает вручную поверх нашего непрерывного
  текста (задача 6: «границы кадров посажены в межсловные паузы»). Конфликт
  практический: перевод нашего сценария в их покадровую модель — ручной шаг
  на каждую сборку, автоматической стыковки двух моделей контента нет.

### Шаг 4 — что из нашего кода лишнее

Подтверждаю/опровергаю три кандидата из решения по факту пилота, не по
предположению — с точными ссылками.

| Кандидат | Вердикт | Подтверждение |
|---|---|---|
| `face_detect.py` | **Лишний, подтверждено** | Уже измерено в задаче 4: `cv2` не установлен и не объявлен зависимостью (`pyproject.toml` проверен целиком), `detect_face_anchor()` при любой ошибке возвращает захардкоженный дефолт `(0.5, 0.42)`, боевой вызов `hf_render.py:237` идёт без детектора. Их `safe-zones.cjs` даёт реальную карту по силуэту тела с разбивкой по яркости/палитре/резкости — измеримо больше сигнала (задача 4, раздел «Сравнение»). |
| гейт ритма (`editplan.validate_plan`, строки 168-199) | **Лишний в HyperFrames-пути, подтверждено** | Функция и её докстрока (`editplan.py:22-23`) сама называет себя устаревшей: «`validate_plan` — старые гейты арифметического rhythm-плана». Единственный вызов `validate_plan(` во всём движке — `__main__.py:292` (старый консольный путь). Боевой HyperFrames-путь (`hf_render.py`) её не импортирует и не вызывает вовсе — там только `hf_gates.check_storyboard` (грид/зоны/лицо, `hf_render.py:22,267`). Реальную проверку на статичность в задаче 6 делал их `check --frame-check "severity=error;seek=.25,.5,.75;tol=2"` — не арифметика по таймкодам, а измерение по кадрам рендера. |
| подбор материала (`broll_retrieval.py` + `broll_library.py`) — «заменяется их `resolve.mjs`» | **Не подтверждено пилотом — гипотеза без проверки** | `broll_retrieval.py:1-17` — ровно семантический подбор b-roll (CLIP-эмбеддинг + косинус к индексу библиотеки), то есть модуль действительно тот. Но задача 6 ни разу не вставляла b-roll (пилотный сценарий — один непрерывный дубль без вставок), `resolve.mjs` в задаче 6 не вызывался вообще. Проверка `resolve.mjs --type video` (`registry.test.mjs:75-82`) показывает провайдеров `heygen.video`/`ltx.local` — это **генерация** видео, не подтверждённый признак семантического поиска по готовой библиотеке клипов, как делает наш модуль. Утверждать замену по факту пилота не могу — это домысел из более раннего решения, не измерение. Нужен отдельный прогон со сценарием, где реально нужна вставка, прежде чем удалять. |

**Что незаменимо** (по факту пилота и по коду — HyperFrames этого не делает
вообще, не вопрос выбора реализации):

- Заказ и оркестрация HeyGen/ElevenLabs (`avatar.py`, `tts.py`, `billing.py`,
  `avatar_islands.py`, `master_audio.py`) — HyperFrames не генерирует
  аватаров и не считает биллинг вендоров, это целиком наш слой.
- `hf_fonts.py` — доказано задачами 1-2: их собственный шрифтовой инжектор
  не несёт кириллицу/казахский на весе, который они выбирают по умолчанию
  (негативный контроль Montserrat 900 сломался ровно на этом).
- Сценарий и текст (`scenario.py`, `llm.py`, `brief.py`) — HyperFrames ничего
  не пишет, только собирает то, что ему дали.
- Телеграм-бот (`bot.py`) — продуктовая поверхность доставки, вне их скоупа
  по определению.
- `transcribe.py` (faster-whisper) — задача 5 не нашла оснований менять:
  слова совпали с их WhisperX-путём буквально один в один на этом материале,
  а их путь требует доп. инфраструктуры (`uv`, ~2 ГБ докачки на первый
  прогон), которой у нас уже нет необходимости заводить.

### Что делать дальше — стоит ли `talking-head-recut`

Прочитан `C:\Users\123\.claude\skills\talking-head-recut\SKILL.md` целиком
(1210 строк) и сопоставлен с опытом сборки задачи 6.

**Да, стоит попробовать на следующем ролике — но не как замену насовсем.**
Главный шов задачи 6 (`assemble-index.mjs` поднимает видео без `z-index`,
графика оказывается под ним, обходной `postassemble.mjs`) структурно не
может повториться в `talking-head-recut`: там нет шага пересборки
композиции вообще. Видео — один top-level `<video>` в `#video-wrap`,
карточки — отдельные `card-host`-слои с `class="clip"`, которые агент сам
пишет в `index.html` поверх видео (строки 918-981 SKILL.md); слой поверх
слоя — просто порядок DOM, а не постобработка чужого файла. Заодно исчезает
и вторая находка задачи 6 — тройной срез голосовой дорожки по границам
кадров: у `talking-head-recut` ролик не режется на кадры-подкомпозиции
вовсе, один непрерывный `<video>`/`<audio>` на весь хронометраж (строки
918-948), то есть ровно тот сценарий пилота (один дубль, без вставок)
собрался бы у них без единого из тех обходов, что заняли половину раздела
«Мины» задачи 6.

**Чем придётся заплатить, если переехать полностью:**
- **Субтитры не их забота.** Формально в брифе и так было известно
  («субтитры отдаёт отдельному скиллу») — подтверждаю по коду:
  `storyboard.json` несёт `subtitles: { enabled: false }` (строка 148
  SKILL.md), а сам скилл прямо отсылает к `/embedded-captions`. Собрать
  реил на `talking-head-recut` — значит гонять два скилла и сводить их
  результат в одну композицию руками, лишней интеграционной работы
  прибавится, а не убавится.
- **Не умеет врезок в другой материал** — тоже было известно заранее,
  подтверждаю: видео «plays in full», раскладка меняется только через
  GSAP-твины positions/bounds одного и того же `<video>` (перекадровка,
  пикчер-ин-пикчер), но не подмена клипа. У нас в разработке ровно
  противоположная возможность — вставка скринкаста как insert-материала
  (коммит `c2ea128 feat(route): record a browser walkthrough as insert
  footage`), `avatar_islands.py` (пропуск аватара там, где вставка вместо
  него) и вся `broll_*` цепочка — все они предполагают **разные** клипы в
  одном ролике. Полный переезд на `talking-head-recut` был бы шагом назад
  именно по этой оси нашей дорожной карты.
- **35 карточных лейаутов под латиницу** — уже отмечено в задаче 6, не
  переоткрываю; шрифты скилла (`Caveat`, `LXGW WenKai TC`, `Inter`,
  `Virgil` — строка 502 SKILL.md) не проверялись на кириллицу отдельно, но
  `Caveat`/`Virgil` — латинские рукописные гарнитуры, риск тот же, что решён
  `hf_fonts.py` для `product-launch-video`, и его придётся решать заново
  для этого скилла, если брать его карточные стили как есть.

**Рекомендация**: следующий ролик без вставок (одиночный говорящий дубль,
как в этом пилоте) — хороший кандидат собрать на `talking-head-recut` +
`embedded-captions` вместо `product-launch-video`, чтобы измерить, действительно
ли исчезает класс шовных багов задачи 6 при сопоставимом визуальном
результате. Не менять инструмент по умолчанию для всего жанра — как только
в сценарии появляется вставка (скринкаст, b-roll), `talking-head-recut`
структурно не подходит.

### Ограничения этого отчёта

- Стоимость (шаг 2) — вилка, не точное число: нет разбивки input/output/кэш
  по переданным агрегатам.
- «Подбор материала → `resolve.mjs`» не подтверждён и не опровергнут по
  факту пилота — сценарий не вставлял b-roll, кандидат остаётся гипотезой.
- Сравнение шага 1 — на 7 статичных кадрах и одном просмотре, не на полном
  покадровом прогоне; для микро-мёртвых зон (доли секунды) это грубая сетка.
- Font-check для карточных гарнитур `talking-head-recut` на кириллицу не
  проводился (нет материала на казахском в этом пилоте, и сама сборка на
  этом скилле не выполнялась) — вывод «риск тот же» логический, не
  измеренный.
